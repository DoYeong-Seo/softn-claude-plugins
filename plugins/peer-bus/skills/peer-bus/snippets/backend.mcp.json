{
  "mcpServers": {
    "peer-bus": {
      "command": "${HOME}/.peer-bus/bin/peer-bus-node",
      "env": {
        "NAME": "backend",
        "PROJECT": "blogn",
        "PEER_BUS_MODE": "poll"
      }
    }
  }
}
